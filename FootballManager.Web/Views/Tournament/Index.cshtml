@model IEnumerable<TournamentViewModel>

@{
    ViewBag.Title = "Tournament List";
}

<h2>Tournament List</h2>

<p>
    <button type="button" class="btn btn-info btn-modal" data-toggle="modal" data-target="#standardModal" data-url="@Url.Action("Create")">
        Create New
    </button>
</p>

<div id="tournamentList">
    @Html.Partial("_TournamentList", Model)
</div>
